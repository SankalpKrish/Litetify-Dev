{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Phase 0 capability set. Scoped per-feature in later phases (auth, playback, mods).",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
